name: tatehama_trainradio
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 現在のアプリで使用しているパッケージ
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_web_auth_2: ^3.0.0
  jwt_decoder: ^2.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # 画像アセットのフォルダを登録
  assets:
    - assets/images/

